{
  "name": "animation-flow",
  "version": "0.0.1",
  "description": "Demo of animation flow.",
  "main": "index.js",
  "scripts": {
    "build": "npm run clean && mkdir -p dist && browserify index.js -o dist/main.js",
    "watch": "npm run clean && mkdir -p dist && watchify index.js -o dist/main.js",
    "clean": "rm -rf dist"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:wko27/animation-flow.git"
  },
  "keywords": [
    "animation",
    "flow"
  ],
  "dependencies": {
    "jquery": "^1.7.2",
    "underscore": "^1.3.1"
  },
  "devDependencies": {
    "browserify": "^14.1.0",
    "watchify": "^3.9.0"
  }
}
