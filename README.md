# Animation Flow

This is a demo that used to run on the engineering public website.

Click on the dots!

To run locally:
- npm install
- npm run build
- ./run.sh (on MacOSX)

Dependencies
- sass (http://sass-lang.com/)
